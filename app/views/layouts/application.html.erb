<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "Booklend" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Favicon and Icons %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= favicon_link_tag asset_path('favicon-16x16.png'), rel: 'icon', type: 'image/png' %>
    <%= favicon_link_tag asset_path('favicon-32x32.png'), rel: 'icon', type: 'image/png' %>
    <%= favicon_link_tag asset_path('apple-touch-icon.png'), rel: 'apple-touch-icon' %>
    <%= favicon_link_tag asset_path('android-chrome-192x192.png'), rel: 'icon', type: 'image/png', sizes: '192x192' %>
    <%= favicon_link_tag asset_path('android-chrome-512x512.png'), rel: 'icon', type: 'image/png', sizes: '512x512' %>
    <%= tag.link rel: "manifest", href: asset_path('site.webmanifest') %>


    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>


<body>
    <%= render "shared/header" %>
    <%= render "shared/flash" %>
    <main class="mt-8 px-5 flex">
        <%= yield %>
    </main>
</body>

</html>