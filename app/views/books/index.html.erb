<div class="max-w-6xl mx-auto mt-10 px-4">
    <h2 class="text-3xl font-bold text-gray-900 mb-6 text-center">📚 All Books</h2>

    <% if @books.any? %>
    <!-- Grid Layout for Cards -->
    <ul class="grid md:grid-cols-3 gap-6">
        <% @books.each do |book| %>
        <%= render partial: "books/book_item", locals: { book: book } %>
        <% end %>
    </ul>
    <% else %>
    <p class="text-gray-500 text-center">No books available at the moment.</p>
    <% end %>
</div>